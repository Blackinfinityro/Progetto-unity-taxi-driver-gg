using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.XR;

public class pupupipi : MonoBehaviour
{ [SerializeField] float velocità = 5f;
    // Start is called before the first frame update
    void Start()
    {
        //InputTrackingState = newinputs();
        //InputTrackingState.Enable();   
    }

    // Update is called once per frame
    void Update()
    {
        //float inputOrizzontale = velocità *Time.deltaTime;
        //InputTrackingState.Giocatore.MuoviOrizzontalmente.ReadValue<float>();
        //transform.Translate((Vector3.right * inputOrizzontale * velocità) * Time.deltaTime);
    
    
        }
}
